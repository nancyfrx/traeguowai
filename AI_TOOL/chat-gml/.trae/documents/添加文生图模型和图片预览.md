## 添加智普文生图模型和图片预览功能

### 问题分析
1. 当前显示 markdown 代码块，需要移除
2. 新对话按钮可能存在事件绑定问题
3. 需要添加文生图模型支持

### 实现步骤

1. **修改 HTML 结构** ([index.html](file:///Users/fengruxue/Documents/chat/index.html))
   - 在模型选择器中添加文生图模型选项（cogview-4、cogview-3-plus）
   - 为每个模型添加标签（文本/图片）
   - 添加图片预览模态框

2. **修改 CSS 样式** ([style.css](file:///Users/fengruxue/Documents/chat/style.css))
   - 添加模型标签样式（文本/图片标签）
   - 添加图片显示样式
   - 添加图片放大预览模态框样式
   - 优化消息内容显示（移除代码块样式）

3. **修改 JavaScript 逻辑** ([script.js](file:///Users/fengruxue/Documents/chat/script.js))
   - 修改 `formatMessage` 函数，移除 markdown 代码块格式化
   - 添加文生图 API 调用函数（调用 `https://open.bigmodel.cn/api/paas/v4/images/generations`）
   - 修改 `sendMessage` 函数，根据选择的模型类型调用不同的 API
   - 添加图片消息显示功能
   - 添加图片点击放大预览功能
   - 修复新对话按钮的事件绑定问题

### 主要功能
- ✅ 移除 markdown 代码块显示
- ✅ 支持文生图模型（CogView 系列）
- ✅ 模型选择器显示类型标签（文本/图片）
- ✅ 对话中可生成和显示图片
- ✅ 点击图片可放大预览
- ✅ 修复新对话功能