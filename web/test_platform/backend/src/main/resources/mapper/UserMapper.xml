<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.testplatform.backend.repository.UserRepository">

    <resultMap id="UserResultMap" type="com.testplatform.backend.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="password" column="password"/>
        <result property="companyId" column="company_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="failedAttempts" column="failed_attempts"/>
        <result property="lockTime" column="lock_time"/>
        <association property="company" column="company_id" select="com.testplatform.backend.repository.CompanyRepository.findById"/>
    </resultMap>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="UserResultMap">
        SELECT * FROM users
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, phone, password, company_id, created_at, failed_attempts, lock_time)
        VALUES (#{username}, #{email}, #{phone}, #{password}, #{companyId}, #{createdAt}, #{failedAttempts}, #{lockTime})
    </insert>

    <update id="update">
        UPDATE users
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="password != null">password = #{password},</if>
            <if test="companyId != null">company_id = #{companyId},</if>
            <if test="failedAttempts != null">failed_attempts = #{failedAttempts},</if>
            <if test="lockTime != null">lock_time = #{lockTime}</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="findByEmail" resultMap="UserResultMap">
        SELECT * FROM users WHERE email = #{email}
    </select>

    <select id="findByPhone" resultMap="UserResultMap">
        SELECT * FROM users WHERE phone = #{phone}
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT count(*) > 0 FROM users WHERE username = #{username}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT count(*) > 0 FROM users WHERE email = #{email}
    </select>

    <select id="existsByPhone" resultType="boolean">
        SELECT count(*) > 0 FROM users WHERE phone = #{phone}
    </select>

</mapper>
