spring.application.name=backend
server.port=8081

# Database Configuration (Default to H2 for quick start if MySQL not ready, or use MySQL if available)
# spring.datasource.url=jdbc:mysql://localhost:3306/test_platform?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=password
# spring.jpa.hibernate.ddl-auto=update

# H2 Configuration (Temporary for development)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
